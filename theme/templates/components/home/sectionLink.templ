package home

import (
	"fmt"
)

templ SectionLink(sectionId string) {
	<div class="flex flex-col items-center font-firamono">
		<a
			href={ fmt.Sprintf("#%s", sectionId) }
			class="flex flex-col gap-1 items-center text-gray-700 w-fit"
		>
			{ children... }
		</a>
	</div>
}
