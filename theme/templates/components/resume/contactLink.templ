package resume

templ ContactLink(text, href string) {
	<li>
		@Link(href) {
			<div class="flex gap2 items-center">
				<div class="grow text-right">{ text }</div>
				<div>
					{ children... }
				</div>
			</div>
		}
	</li>
}
